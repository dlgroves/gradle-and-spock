subprojects {
    apply plugin: 'java'
    apply plugin: 'groovy'

    repositories {
        jcenter()
    }

    sourceCompatibility = 1.7
    targetCompatibility = 1.7

    group = 'com.groves.douglas'
    version = 0.1
}